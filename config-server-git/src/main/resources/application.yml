spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shitongtong/spring-cloud-study/
          search-paths: config-repo
server:
  port: 1201